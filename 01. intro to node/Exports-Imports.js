/*
 Export commands:
   module.exports = myFunc; / {myFunc1, myFunc2, myFunc3...};

Import commands:
   const myFunc = require(./fileName path);
  */;
  
  const url = new URL ("http://localhost:3000/catalog/");
  console.log(url);